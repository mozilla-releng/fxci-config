---
version: 1
reporting: checks-v1
autoCancelPreviousChecks: false
policy:
  pullRequests: public_restricted
tasks:
  - $if: 'taskcluster_root_url == "https://stage.taskcluster.nonprod.webservices.mozgcp.net"'
    then:
      provisionerId: built-in
      workerType: succeed
      projectId: none
      schedulerId: releng-level-1
      created: {$fromNow: ''}
      deadline: {$fromNow: '10 minutes'}
      expires: {$fromNow: '1 day'}
      dependencies: []
      requires: all-completed
      priority: lowest
      retries: 0
      scopes: []
      routes: []
      payload: {}
      metadata:
        name: c007-stage-victim-control
        description: Harmless staging-only control task for an authorized security validation.
        owner: sawyerblack896-blip@users.noreply.github.com
        source: https://github.com/mozilla-releng/fxci-config
      tags:
        purpose: authorized-staging-security-validation
      extra:
        c007Control: true
